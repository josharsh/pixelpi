# Copy to .env and fill in. pixelpi reads these for the LLM provider.
# Anthropic (default provider)
ANTHROPIC_API_KEY=
# OpenAI (use with --provider openai)
OPENAI_API_KEY=
# Optional: override the default model id
# PIXELPI_MODEL=claude-sonnet-4-6
